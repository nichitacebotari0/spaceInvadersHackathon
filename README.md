for macos:
- brew install sdl3
- run build.sh

for windows:
- download sdl and edit build.bat to point to install location
- copy the sdl3.dll from downloaded and overwrite the one in game folder
- run build.bat